### Copy this file to `.env` and fill values with your secrets. Do NOT commit `.env` to version control.
### Example .env template for DBcolor

# MongoDB Atlas connection string (do NOT commit your actual credentials)
MONGO_URI=mongodb+srv://<USERNAME>:<PASSWORD>@cluster0.mongodb.net/dbdouble?retryWrites=true&w=majority

# WebSocket URL for Playna (or your WS vendor)
PLAYNABETS_WS_URL=wss://play.soline.bet:5903/Game

# App secret for JWT and sensitive operations - generate a strong secret
SECRET_KEY=change-me-to-a-strong-secret

# JWT expiration in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Port used by the server in production
PORT=3001

# Martingale config
MARTINGALE_ENABLED=true
MARTINGALE_MAX_ATTEMPTS=3
BLOCK_SIGNALS_WHILE_PENDING=true

# Optional: If you want to set the DB name explicitly
DB_NAME=dbdouble
